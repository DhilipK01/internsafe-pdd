abstract final class AppDurations {
  static const Duration splash = Duration(seconds: 2);
  static const Duration scan = Duration(seconds: 3);
  static const Duration shortAnim = Duration(milliseconds: 300);
  static const Duration mediumAnim = Duration(milliseconds: 500);
}
